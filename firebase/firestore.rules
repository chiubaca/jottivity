rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  	
    // only let users who are signed in to write to the /test collection
    match /users/{data}{
       // allow read, write: if request.auth != null;
       allow read,write
    }
  }
}